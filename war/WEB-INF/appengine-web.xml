<?xml version="1.0" encoding="utf-8"?>
<appengine-web-app xmlns="http://appengine.google.com/ns/1.0">
  <application>meetmesncftest</application>
  <version>1</version>

  <!--
    Allows App Engine to send multiple requests to one instance in parallel:
  -->
  <threadsafe>true</threadsafe>

  <!-- Configure java.util.logging -->
  <system-properties>
    <property name="java.util.logging.config.file" value="WEB-INF/logging.properties"/>
  </system-properties>

  <!--
    HTTP Sessions are disabled by default. To enable HTTP sessions specify:

      <sessions-enabled>true</sessions-enabled>

    It's possible to reduce request latency by configuring your application to
    asynchronously write HTTP session data to the datastore:

      <async-session-persistence enabled="true" />

    With this feature enabled, there is a very small chance your app will see
    stale session data. For details, see
    http://code.google.com/appengine/docs/java/config/appconfig.html#Enabling_Sessions
  -->
  <sessions-enabled>true</sessions-enabled>
  <!-- 
    On indique a google les fichiers statiques pour que celui-ci puisse les mettre 
    sur des serveurs specifiques. Google peut ainsi les dupliquer sur plusieurs 
    serveurs via un CDN (Content Delivery Network). Ainsi, chaque visiteur ira 
    recuperer les fichiers statiques sur le serveur le plus proche de lui, ce qui
    rendra leur telechargement plus rapide.
  -->
  <static-files>
  	<include path="/*.ico" />
    <include path="/js/*.js" />
    <include path="/css/*.css" />
    <include path="/fonts/*.eot" />
    <include path="/fonts/*.svg" />
    <include path="/fonts/*.ttf" />
    <include path="/fonts/*.woff" />
  </static-files>
</appengine-web-app>
